import React from 'react';
import { useParams } from 'react-router-dom';

const Purchasepage = () => {
    const { id } = useParams()
    return (
        <div>
               hello
        </div>
    );
};

export default Purchasepage;